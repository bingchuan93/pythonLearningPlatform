import MainApp from '/imports/ui/layouts/mainApp';

const contentRoutes = [{
    name: 'main-app',
    path: [
        '/',
    ],
    exact: true,
    component: MainApp
}];

export default contentRoutes;